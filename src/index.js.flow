// @flow

declare function throttle<Args: Array<any>, Value>(
  fn: (...args: Args) => Promise<Value>,
  wait: ?number,
  options?: {
    getNextArgs?: (args0: Args, args1: Args) => Args
  }
): (...args: Args) => Promise<Value>;

declare module.exports: typeof throttle;
